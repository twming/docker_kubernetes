# TODO : enter your YAML here, to convert to JSON object --> 

# (Terminal 1)
# kubectl apply -f pod.yaml
# kubectl get pods
# kubectl expose pod myanalysis --port=8888
# kubectl get all
# minikube service myanalysis

# kubectl delete -f pod.yaml
# kubectl delete service myanalysis
# kubectl get all

# (Terminal 2)
# minikube dashboard
